[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pproc-cascade"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "earthkit-meteo>=0.1.1",
    "filelock>=3.12.0",
    "numexpr",
    "dill",
    "cython",
    "scipy",
    "code-meters",
    "earthkit-data @ git+ssh://git@github.com/ecmwf/earthkit-data.git@develop",
    "cascade-python @ git+ssh://git@github.com/ecmwf/cascade.git",
    "pyfdb @ git+https://github.com/ecmwf/pyfdb.git@master",
    "pproc @ git+ssh://git@github.com/ecmwf/pproc.git@1.2.0"
]

[project.optional-dependencies]
tests = ["pytest"]
lint = ["black", "isort", "flake8"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.isort]
profile = "black"